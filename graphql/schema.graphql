type Movie {
    id: Int!,
    title: String!,
    summary: String!,
    rating: Float!,
    language: String!,
    medium_cover_image: String!
}

type Query {
    movies(limit: Int, rating: Float): [Movie]!
}
